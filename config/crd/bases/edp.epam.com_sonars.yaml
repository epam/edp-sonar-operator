---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: sonars.edp.epam.com
spec:
  group: edp.epam.com
  names:
    kind: Sonar
    listKind: SonarList
    plural: sonars
    singular: sonar
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Sonar is the Schema for the sonars API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SonarSpec defines the desired state of Sonar.
            properties:
              basePath:
                type: string
              defaultPermissionTemplate:
                type: string
              groups:
                description: Groups specify which groups should be created.
                items:
                  properties:
                    name:
                      type: string
                    permissions:
                      items:
                        type: string
                      type: array
                  required:
                  - name
                  type: object
                type: array
              plugins:
                description: Plugins specify which plugins should be installed to
                  sonar.
                items:
                  type: string
                type: array
              qualityGates:
                description: QualityGates specify which quality gates should be created.
                items:
                  properties:
                    conditions:
                      items:
                        properties:
                          error:
                            type: string
                          metric:
                            type: string
                          op:
                            type: string
                          period:
                            type: string
                        required:
                        - error
                        - metric
                        - op
                        type: object
                      type: array
                    name:
                      type: string
                    setAsDefault:
                      type: boolean
                  required:
                  - conditions
                  - name
                  type: object
                type: array
              secret:
                description: Secret is the name of the k8s object Secret related to
                  sonar.
                type: string
              settings:
                description: Settings specify which settings should be configured.
                items:
                  properties:
                    key:
                      type: string
                    value:
                      type: string
                    valueType:
                      type: string
                  required:
                  - key
                  - value
                  - valueType
                  type: object
                type: array
              url:
                description: Url is used to explicitly specify the url of sonar. It
                  may not be needed if the sonar is deployed in the same cluster.
                type: string
              users:
                description: Users specify which users should be created.
                items:
                  properties:
                    group:
                      type: string
                    login:
                      type: string
                    permissions:
                      items:
                        type: string
                      type: array
                    username:
                      type: string
                  required:
                  - login
                  - username
                  type: object
                type: array
            required:
            - secret
            type: object
          status:
            description: SonarStatus defines the observed state of Sonar.
            properties:
              available:
                type: boolean
              externalUrl:
                type: string
              lastTimeUpdated:
                format: date-time
                type: string
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
