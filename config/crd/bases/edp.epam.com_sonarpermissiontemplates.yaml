---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: sonarpermissiontemplates.edp.epam.com
spec:
  group: edp.epam.com
  names:
    kind: SonarPermissionTemplate
    listKind: SonarPermissionTemplateList
    plural: sonarpermissiontemplates
    singular: sonarpermissiontemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SonarPermissionTemplate is the Schema for the sonar permission
          template API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SonarPermissionTemplateSpec defines the desired state of
              SonarPermissionTemplate.
            properties:
              description:
                description: Description of sonar permission template.
                type: string
              groupPermissions:
                description: GroupPermissions adds a group to a permission template.
                items:
                  description: GroupPermission represents the group and its permissions.
                  properties:
                    groupName:
                      description: Group name or 'anyone' (case insensitive). Example
                        value sonar-administrators.
                      type: string
                    permissions:
                      description: 'Permissions is a list of permissions. Possible
                        values: admin, codeviewer, issueadmin, securityhotspotadmin,
                        scan, user.'
                      items:
                        type: string
                      type: array
                  required:
                  - groupName
                  - permissions
                  type: object
                nullable: true
                type: array
              name:
                description: Name is a group name.
                type: string
              projectKeyPattern:
                description: ProjectKeyPattern is key pattern. Must be a valid Java
                  regular expression.
                type: string
              sonarOwner:
                description: SonarOwner is a name of root sonar custom resource.
                type: string
            required:
            - name
            - projectKeyPattern
            - sonarOwner
            type: object
          status:
            description: SonarPermissionTemplateStatus defines the observed state
              of SonarPermissionTemplate.
            properties:
              failureCount:
                format: int64
                type: integer
              id:
                type: string
              value:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
