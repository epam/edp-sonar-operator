global:
  edpName: ""
  platform: "openshift"
  dnsWildCard: "delivery.aws.main.edp.projects.epam.com"
  openshift:
    deploymentType: "deploymentConfigs"

name: sonar-operator
annotations: {}
nodeSelector: {}
tolerations: []
affinity: {}
image:
  name: epamedp/sonar-operator
  # if not defined then .Chart.AppVersion is used
  version:
imagePullPolicy: "IfNotPresent"

resources:
  limits:
    memory: 192Mi
  requests:
    cpu: 50m
    memory: 64Mi

sonar:
  initContainers:
    # image: busybox:1.32
    # We allow the init containers to have a separate security context declaration because
    # the initContainer may not require the same as SonarQube.
    # securityContext: {}
    # We allow the init containers to have a separate resources declaration because
    # the initContainer does not take as much resources.
    resources: {}
  deploy: true
  name: "sonar"
  image: "sonarqube"
  version: "8.9.2-community"
  imagePullPolicy: "IfNotPresent"
  initImage: "busybox"
  basePath: ""
  imagePullSecrets:
  storage:
    data:
      class: gp2
      size: 1Gi
    database:
      class: gp2
      size: 1Gi
  annotations: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}
  ingress:
    annotations: {}
    # pathType is only for k8s >= 1.1=
    pathType: Prefix
    # For Kubernetes >= 1.18 you should specify the ingress-controller via the field ingressClassName
    # See https://kubernetes.io/blog/2020/04/02/improvements-to-the-ingress-api-in-kubernetes-1.18/#specifying-the-class-of-an-ingress
    # ingressClassName: nginx
  sonarqubeFolder: /opt/sonarqube
  # List of plugins to install.
  # For example:
  plugins:
    install:
      - "https://github.com/vaulttec/sonar-auth-oidc/releases/download/v2.0.0/sonar-auth-oidc-plugin-2.0.0.jar"
      - "https://github.com/checkstyle/sonar-checkstyle/releases#:~:text=checkstyle-sonar-plugin-8.45.1.jar"

  resources:
    limits:
      memory: 3Gi
    requests:
      cpu: 100m
      memory: 1.5Gi

  db:
    image: "postgres:9.6"
    imagePullPolicy: "IfNotPresent"
    resources:
      limits:
        memory: 512Mi
      requests:
        cpu: 50m
        memory: 64Mi
    annotations: {}
    nodeSelector: {}
    tolerations: []
    affinity: {}
